apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
  namespace: nginx-app
  labels:
    app: my-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: my-app
        image: arminsabzehi/my-app:v2
        ports:
        - containerPort: 80
